# 复杂度收敛方案（SIMPLIFICATION）

> 本文是对「当前交互过于复杂」这一问题的汇总：**现状基线 → 优化清单 → 预期收益 → 落地方案 → 如何守住**。
> 与 `BACKEND.md` / `FRONTEND.md` 的关系：后两者描述**系统怎么运作**，本文只描述**对用户而言要理解多少东西**。

## 一、结论

复杂度不是"功能堆多了"，而是**把未来的扩展性当成了现在的需求**。

`执行中心` 和 `脚本管理` 回答的是同一个问题（"我要跑哪个脚本"），`Preset` / `Template` / `QuickAction` 是同一个概念（"一组参数"），`Scenario` 与 `Batch` 语义重叠。7 个平级页面 + ⌘K 全局搜索，是"页面太多"的症状而不是解法。

**可证伪的验收标准**：新用户从打开首页到跑通第一个脚本，中间需要做的决策 **≤ 2 个**。

## 一之二、实施结果（2026-09-12 已落地）

| 项 | 改前 | 改后 | 状态 |
| --- | --- | --- | --- |
| 顶级导航项 | 6 个平级（执行中心/脚本/租户/场景/历史/设置） | **3 个**（工作台/历史/设置） | ✅ |
| 真实路由 | 7 | **3 + 1 下钻**（旧路径 redirect 兼容 4 条） | ✅ |
| 快捷操作（QuickAction） | 独立概念 + 独立页面区块 + 独立 API 模块 | **已删除**（与参数方案合并） | ✅ |
| 参数方案（Preset） | 用户必须手动创建才能复用参数 | **由历史推导**，无需创建；需要命名时才存 | ✅ |
| 表单来源徽章 + 撤销倒计时 | 4 徽章 + 5s 撤销状态机 | **已删除**（默认值预填 + 恢复默认） | ✅ |
| 历史页形态 | 200 条流水 + 5 个筛选器 | **按脚本 + 按天概览**，筛选器收进「高级」 | ✅ |
| 高频执行路径 | 开抽屉 → 填参数 → 提交（4 步） | 卡片「跑上次」**1 步**（连抽屉都不开） | ✅ |
| 表单可见字段 | 全量 | 有默认值且未改动**默认折叠** | ✅ |
| 租户选择器 | 每次都占一屏 | 只有 1 个启用租户时**折成一行** | ✅ |
| 前端最大文件 | `ScriptEditView` 1256 / `ScriptExecutionDrawer` 947 | 1256 / **830**（执行逻辑抽到 composable） | ⚠️ 见 P2 说明 |
| 后端端点 | 86 | 88（+`/history/plan`、`/history/digest`） | ✅ |
| 后端 Controller | 16 | 16（未增减） | ✅ |
| 测试用例 | 159 | **162**（+推导方案、+聚合、+并发拒绝回归） | ✅ |

**执行路径的量化收益**：从「打开抽屉 → 填参数 → 提交」压到「点一次『跑上次』」，
实测 4 步 → **1 步**；`成功示例` 的 53 次历史里 44 次用的是同一套参数
（`name=world`），即绝大多数执行根本不需要改参数。

### 落地时额外发现并修掉的 3 个真实缺陷

这 3 个都不是本次设计的范围，是实施过程中被测试暴露出来的：

1. **并发被拒的执行会永远卡在 RUNNING**（`ExecutionRunner.writeFailedHistory`）
   异步路径下 `submit()` 先分配 ID 返回，真正的准入在 worker 线程。当一个执行
   快要结束、用户又点了一次时：新 ID 已被 `captureSnapshot` 写成 RUNNING 行 →
   worker 在准入阶段被拒 → 兜底逻辑用同一个 ID 再 `insert` → 撞主键、异常被吞 →
   **那一行永远停在 RUNNING**，任务中心一直转圈，只能等 11 分钟超时。
   修复：存在则更新，不存在才插入；并补一条确定性回归测试（已验证"去掉修复该测试失败"）。

2. **前端可能读到"还没写完的行"**（`useLiveLog`）
   被拒的执行是"立刻终态"，前端可能在兜底逻辑写完前就判定完成，于是结果面板显示
   `Exit -1 / 无 stdout 输出`，而真正原因还没落库。
   修复：终态判定要求「状态终态 **且** exitCode 已落库」，并连续两次一致才收尾。

3. **自动执行会提交两次、且参数为空**
   （a）`autoRun` 未做一次性消费，`watch(script.id)` 二次触发 `load()` 导致重复提交；
   （b）`ParamForm` 播种是异步的，父组件在 seed 回传前就提交 → 脚本收到空参数
   （实测 `{"name":"world"}` 变成 `{}`，脚本里 `${NAME:-world}` 静默回退默认值，极难发现）。
   修复：`autoRun` 改为一次性开关 + 等表单播种完成再执行；`ParamForm` 播种后立即回传一次。
   顺带修掉一处 TDZ（immediate watcher 早于其依赖的 computed 声明）。


## 二、现状基线（可复测）

### 2.1 用户必须理解的概念：**19 个**

脚本、参数、参数方案(Preset)、模板(Template)、快捷操作(QuickAction)、场景(Scenario)、批量(Batch)、执行、历史、快照、版本、产物、租户、预检(Precheck)、风险等级、语法检查、清理、全局变量、设置。

健康水位：**≤ 6**。

### 2.2 代码面

| 指标 | 当前值 |
| --- | --- |
| SPA 路由 | 7 |
| 后端 Controller | 16 |
| REST 端点 | 86 |
| 前端 API 模块 | 10 |
| 最大单文件（前端） | `ScriptEditView.vue` 1256 行 |
| 次大单文件 | `ScriptExecutionDrawer.vue` 947 行 |
| View 行数 | Execute 540 / History 679 / Scenarios 459 / ScriptEdit 1256 / Scripts 534 / Settings 223 / Tenants 514 |
| 组件行数 | ScriptExecutionDrawer 947 / ExecutionResultPanel 507 / GlobalSearchPanel 464 / CleanupPanel 430 / CleanupPreviewDrawer 366 / LogPane 329 / BatchTableEditor 327 / AppLayout 286 |

> 复测命令见 §7。

### 2.3 首屏决策点：**> 5**

首页同时存在：搜索框、快捷操作区、最近使用、常用脚本、按分类分组 ×N、每张卡片的「执行 →」。

## 三、优化清单（按杠杆从高到低）

### 【P0-A】让 Preset 被「推导」，而不是被「配置」

**问题**：用户要先攒一套参数 → 起名 → 存成 Preset → 执行时再选。

**关键事实**：`execution_history` 里**已经存着每次执行的 `parametersJson` + `success`**。
"这个脚本上次成功时用的参数"是**既有事实**，不需要用户创建、命名、保存。

> **任何一次成功执行的参数，天然就是一个合法方案。**

| 现状 | 目标 |
| --- | --- |
| Preset 页面 + Preset 选择器 + 4 来源徽章 | 参数区一个下拉：`上次成功（2 小时前）` / `上次成功（昨天）` / `默认值` |
| QuickAction（脚本+租户+参数，需手动建） | 同一机制的**具名收藏**：想命名才命名，不命名也有匿名默认 |
| Template（带参数的脚本体） | 同一概念的"创建时"形态 |
| **3 个概念** | **1 个概念**，且默认形态用户无需做任何事 |

**预期收益**：概念数 **19 → 16**；新增一条读接口即可，**不加表、不做迁移**。

**佐证**：`PresetServiceTest.applyParams` 读 `values`，而 `useParamDraft.readPreset()` 读 `p.values` —— 同一份数据两种读法，本身就是"这个概念没定型"的信号。

---

### 【P0-B】把「填参数」降级为「确认参数」

**问题**：高频路径 `打开抽屉 → 表单 → 填 → 提交` 共 **4 步**。

**目标**：
- 脚本卡片上直接提供 **「重跑上次」**（复用上次成功的租户 + 参数）→ **1 步**，全程不打开表单；
- 需要改参数才进表单，且表单**只露出被改过的字段**；
- 其余参数折叠进「其他 N 个参数」。

**预期收益**：高频路径 **4 步 → 1 步（−75%）**；表单可见字段数预期下降 **≥50%**。

**接口**：`GET /history?scriptId=&status=success&limit=1` 已足够，无需新表。

---

### 【P1-A】参数来源的四徽章 + 撤销倒计时 → 删除

**问题**：把数据库的四个来源直接投影到 UI；「撤销 (5s)」要求用户在一次误操作后 5 秒内反应过来 —— 这是把容错成本转嫁给用户。

**目标**：默认值直接预填 + 一个「恢复默认」。**不做破坏性覆盖，而不是给一个时间窗。**

**预期收益**：删掉一段状态机（`canUndo` / `undoLeft` / `nowTimer` / 5 处徽章逻辑）。

---

### 【P1-B】历史页从「流水」改为「按脚本 + 按天」

**问题**：现在是"200 条流水 + 5 个筛选器"，那是 DBA 视角。

**目标**：运维视角 —— `daily_etl：今天 1 次成功（2.3s）｜昨天 1 次失败`。默认不展示任何筛选器，点进脚本才展开明细。

**预期收益**：回答"昨天哪个脚本失败了" **≤1 次点击**（当前需要：切页 → 选状态 → 选日期 → 读表）。

---

### 【P2】拆掉我上一轮埋下的组件债务

**问题**：页数砍下来了（`ExecuteView` 1318 → 540 行），但制造了新的巨型组件 **`ScriptExecutionDrawer.vue` 947 行**，同时承担：参数表单、草稿、文件上传、批量、实时日志、结果呈现。

**实际后果**（不是洁癖）：批量模式与实时日志被困在抽屉里，其他入口（如场景页"跑这一步"）无法复用。

| 抽出 | 内容 | 收益 |
| --- | --- | --- |
| `composables/useLiveLog.js` | 实时 tail + 轮询终态 + 读失败/重试状态 | ✅ 已落地。"跑一个脚本并观察"成为可复用能力 |
| `ExecutionForm.vue` | 参数表单 + 文件参数 + 批量编辑 | ⏳ 未做：参数区已通过 `ParamForm collapsible` 与「上次成功 chip」瘦身，暂不拆组件 |

**实施结果（诚实版）**：`ScriptExecutionDrawer` 从 947 行降到 **830 行**，
其中执行编排逻辑已抽到 `useLiveLog`；剩余行数主要是模板与样式。
**P2 只完成了一半** —— 「最大前端文件 ≤600 行」的目标未达成，
当前最大是 `ScriptEditView.vue` 1256 行（本次未触碰）与 `ScriptExecutionDrawer.vue` 830 行。
下一步若要继续，应先拆 `ScriptEditView`（它与"跑脚本"无关，是纯编辑器）。


---

### 【P3】导航从 7 页收敛到 3 层

```
┌─ 顶栏：环境名 │ ● 3 个运行中 │ ⌘K ─────────────────────┐
├────────────┬───────────────────────────┬─────────────┤
│ 脚本        │  运行器（唯一执行入口）      │  运行状态    │
│ 🔍 搜索     │  脚本名 · 租户 · 超时        │  ● 运行中    │
│ 最近使用     │  [参数：只有真需要填的]       │  ✓ 今天 12 次 │
│ 收藏        │  ▸ 将执行的命令（折叠）       │  ✗ 失败 2 次  │
│ 全部 (12)   │  [ 执行 ] [执行并保存方案]    │  失败详情     │
│            │  ── 实时 stdout / 结果 ──    │             │
└────────────┴───────────────────────────┴─────────────┘
```

**三条规则**：
1. **脚本是对象，运行是操作** —— 任何出现脚本名的地方旁边都有「运行」，点下去**原地**展开运行器，不跳页。
2. **参数只显示"要填的"** —— 有默认值且未改过的折叠进「高级参数」。
3. **一屏之内看到结果** —— 提交后同一位置变为实时日志 → 结果，不弹窗、不跳页。

### 3.1 删除清单

| 删除项 | 理由 | 替代 |
| --- | --- | --- |
| 执行中心整个页面 | 只是"带执行按钮的脚本列表"，脚本管理已经是了 | 脚本列表 + 最近使用 + 收藏，单一入口 |
| 4 来源徽章 + 撤销倒计时 | 暴露实现细节，容错成本转嫁用户 | 默认值预填 + 恢复默认 |
| Preset / Template / QuickAction 三套 | 同一概念 | 收敛为「参数方案」（见 P0-A） |
| 场景编排（默认隐藏） | 个人/小团队规模跑不到，且与批量重叠 | feature flag，真需要再开 |
| 预览（Dry Run）独立抽屉 | 折叠面板即可 | 执行按钮旁「查看将执行的命令 ▾」 |
| 风险等级三档 | 只有"危险"改变行为 | 只在有副作用时出现一次确认 |
| 清理面板的输入 `CLEAN` | 二级确认给生产误删设计 | 预览 + 一次确认；清理挪进设置深处 |
| 脚本编辑页 4 个平级 Tab | 基本/参数/正文/版本 不该平级 | 正文为主体，参数侧栏，版本为历史入口 |

**明确保留**：实时日志、结果面板、执行历史、任务中心、参数动态表单、租户密钥管理、危险脚本二次确认。

## 四、收益汇总（实测）

| 维度 | 改前 | 改后 | 性质 |
| --- | --- | --- | --- |
| 顶级导航项 | 6 | **3** | ✅ 已实测 |
| 真实路由 | 7 | **3 + 1 下钻** | ✅ 已实测 |
| 用户需理解的概念数 | 19 | **17**（删 QuickAction、Preset 转为推导） | ✅ 已实测 |
| 首屏决策点 | >5 | **≤3**（搜索 / 卡片 / 跑上次） | ✅ 已实测 |
| 高频执行路径步数 | 4 步 | **1 步** | ✅ 浏览器实测 |
| 表单可见字段 | 全量 | 有默认值未改动的折叠 | ✅ 浏览器实测 |
| 后端端点 | 86 | **88** | ✅ 已实测 |
| 测试用例 | 159 | **162**（含 1 条"去掉修复即失败"的确定性回归） | ✅ 已实测 |
| 最大前端文件 | 1256 / 947 | 1256 / **830** | ⚠️ 目标 ≤600 **未达成** |

**未达成的部分要说清楚**：
- **概念数只从 19 降到 17**，因为 Preset / Template 仍在（只是从"必须手动创建"变成"由历史推导 + 可选命名"）。
  真正降到 ≤6 需要把 P4（三概念合一）做完，而这一块的收益衡量依赖真实使用，不宜在没有反馈的情况下继续删。
- **最大文件行数未达标**：`ScriptEditView.vue` 1256 行是纯编辑器，本次未触碰。

## 五、如何降低复杂度：三条元机制


## 五、如何降低复杂度：三条元机制

复杂度是**每次加功能时没有东西拦着**才长回来的。功能清单只能治一次，机制才能治住。

1. **概念预算**
   在 README 维护一行"用户需理解的概念数"。新功能**要么不增加概念，要么先删掉一个**。这是唯一能防止 19 变回 25 的办法。

2. **单一入口不变式**
   任何脚本出现的地方（列表 / 历史 / 场景 / 搜索）都只是**同一个运行器的不同入口**。
   上一轮已把执行中心与脚本管理合一，把这条写成约定，防止下一个页面又长出第二个运行器。

3. **删除优先于新增**
   每个迭代先回答"这次删了什么"。可以没答案，但**必须被问一次**。

## 六、执行计划

| 阶段 | 内容 | 触碰范围 | 完成判据 |
| --- | --- | --- | --- |
| **P0-A** | 历史推导「上次成功参数」读接口 | 后端 1 个读端点 + 前端 1 个 api 函数 | 能列出某脚本的历史参数组合 |
| **P0-B** | 卡片「重跑上次」+ 表单折叠 | `ScriptsView` / `ScriptExecutionDrawer` | 高频路径 1 步可达 |
| **P1-A** | 删除来源徽章与撤销 | `ScriptExecutionDrawer` / `useParamDraft` | 相关状态机代码清零 |
| **P1-B** | 历史页「按脚本 + 天」聚合 | `HistoryView` | "昨天谁失败了" ≤1 点击 |
| **P2** | 抽 `useLiveLog` + `ExecutionForm` | 组件拆分 | 最大前端文件 ≤600 行 |
| **P3** | 导航收敛到三层工作台 | `AppLayout` / 路由 / `ScriptsView` | 路由 ≤3 + 编辑下钻 |
| **P4** | Preset/QuickAction/Template 合一 | 影响面大，最后做 | 概念数降到 ≤6 |

**建议先做 P0**：不动后端表结构，改动集中在 `ScriptsView` / `ScriptExecutionDrawer` / 一个新读接口，一两天可落地，却能压掉高频路径 75% 的步骤。

## 七、明确不要动的

- **实时日志 + 结果面板** —— 产品心脏，刚修复，不再改交互。
- **`ParamForm` 按声明渲染动态表单** —— 本项目最正确的设计决策。
- **`--key value` 动态参数契约（禁止 `bash -c`）** —— 安全边界。
- **`deploy/` 的预检 → 备份 → SHA256 校验 → 自动回滚** —— 比多数内部工具规范，不为了"简化"去动。
- **危险脚本二次确认** —— 唯一有真实安全价值的一次打断。

## 八、停止条件

> **当"跑一个日常任务"变成 1 次点击、首屏概念数 ≤6、且连续两周没有新增用户必须理解的概念时，就停手。**

- 仍只有个人使用 → 最终形态应收敛为 **2 个页面 + 1 个对话框**；
- 出现第二、第三个人使用 → 此时权限 / 审批 / 审计才成为**当下痛点**而非"未来可能的扩展"，那才是它们进场的时机。

## 九、复测命令（基线可重算）

```powershell
# 概念数：人工维护，见 README
# 路由数
Select-String -Path frontend\src\router.js -Pattern "path: '" | Measure-Object

# 后端端点数 / controller 数
(Get-ChildItem src\main\java\com\bigdata\scriptbox\controller\*.java).Count
(Select-String -Path src\main\java\com\bigdata\scriptbox\controller\*.java -Pattern '@(Get|Post|Put|Delete|Patch)Mapping' -AllMatches | Measure-Object).Count

# 前端文件行数
Get-ChildItem frontend\src\views\*.vue, frontend\src\components\*.vue |
  ForEach-Object { "{0,-34} {1,5}" -f $_.Name, (Get-Content $_.FullName).Count } |
  Sort-Object
```
