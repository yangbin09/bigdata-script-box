<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bigdata.scriptbox.mapper.GlobalVariableMapper">

    <resultMap id="BaseResultMap" type="com.bigdata.scriptbox.entity.GlobalVariable">
        <id     column="id"             property="id"/>
        <result column="variable_key"   property="variableKey"/>
        <result column="variable_value" property="variableValue"/>
        <result column="description"    property="description"/>
        <result column="sensitive"      property="sensitive"/>
        <result column="enabled"        property="enabled"/>
        <result column="create_time"    property="createTime"/>
        <result column="update_time"    property="updateTime"/>
    </resultMap>

    <select id="selectEnabled" resultMap="BaseResultMap">
        SELECT id, variable_key, variable_value, description, sensitive, enabled, create_time, update_time
        FROM global_variable
        WHERE enabled = TRUE
        ORDER BY id ASC
    </select>

</mapper>